# Django + Angular 專案

這是一個 Django 後端 + Angular 前端的混合專案範例，適合上傳 GitHub。

---

## 專案結構

## 1️⃣ Django 後端

### 安裝套件
```bash
cd med-table
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt